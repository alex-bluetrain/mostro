{
  "outcomes": [
    {
      "label": "refunds.acknowledged",
      "condition": "<describir cuándo un mail es el acuse de recibo de la solicitud de reintegro (recibida, todavía sin aprobar)>",
      "examples": {
        "match": [
          "<fragmento real de un mail de acuse del procesador>"
        ],
        "no_match": [
          "<fragmento real de un mail del procesador que NO es un acuse>"
        ]
      }
    },
    {
      "label": "refunds.approved",
      "condition": "<describir cuándo un mail confirma la aprobación del reintegro, con número/código de referencia>",
      "examples": {
        "match": [
          "<fragmento real de un mail de aprobación>"
        ],
        "no_match": [
          "<fragmento real de un mail que NO es una aprobación>"
        ]
      },
      "extract": {
        "type": "object",
        "properties": {
          "refundReference": {
            "type": "string",
            "description": "Número o código de referencia del reintegro tal como aparece en el mail"
          }
        },
        "required": ["refundReference"],
        "additionalProperties": false
      }
    },
    {
      "label": "refunds.deposited",
      "condition": "<describir cuándo un mail avisa que el depósito del reintegro se acreditó, con monto y fecha>",
      "examples": {
        "match": [
          "<fragmento real de un mail de depósito acreditado>"
        ],
        "no_match": [
          "<fragmento real de un mail que NO es un aviso de depósito>"
        ]
      },
      "extract": {
        "type": "object",
        "properties": {
          "depositAmount": {
            "type": "number",
            "description": "Monto depositado en pesos, sin símbolo de moneda"
          },
          "depositDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Fecha del depósito en formato YYYY-MM-DD"
          }
        },
        "required": ["depositAmount", "depositDate"],
        "additionalProperties": false
      }
    }
  ],
  "default-outcome": {
    "label": "refunds.unknown"
  }
}
