{
  "outcomes": [
    {
      "label": "diapers.confirmed",
      "condition": "<describir en lenguaje natural cuándo un mail confirma el pedido de pañales: qué dice el proveedor, qué datos incluye>",
      "examples": {
        "match": [
          "<fragmento real de un mail del proveedor que SÍ es una confirmación>"
        ],
        "no_match": [
          "<fragmento real de un mail del proveedor que NO es una confirmación (ej: rechazo, aviso de validación)>"
        ]
      },
      "extract": {
        "type": "object",
        "properties": {
          "deliveryDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Fecha de entrega en formato YYYY-MM-DD. Resolver el año o fechas relativas ('mañana', 'el martes') usando la fecha de recepción del mail."
          },
          "deliveryAddress": {
            "type": "string",
            "description": "Domicilio de entrega completo tal como aparece en el mail"
          },
          "quantity": {
            "type": "number",
            "description": "Cantidad de unidades a entregar, número entero"
          }
        },
        "required": ["deliveryDate", "deliveryAddress", "quantity"],
        "additionalProperties": false
      }
    }
  ],
  "default-outcome": {
    "label": "diapers.unknown"
  }
}
