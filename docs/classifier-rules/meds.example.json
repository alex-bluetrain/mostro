{
  "outcomes": [
    {
      "label": "meds.acknowledged",
      "condition": "<describir cuándo un mail es el acuse de recibo del pedido de medicamentos (la farmacia recibió el pedido, todavía sin entrega)>",
      "examples": {
        "match": [
          "<fragmento real de un mail de acuse de la farmacia>"
        ],
        "no_match": [
          "<fragmento real de un mail de la farmacia que NO es un acuse>"
        ]
      }
    },
    {
      "label": "meds.delivered",
      "condition": "<describir cuándo un mail confirma la entrega de los medicamentos, con fecha y domicilio>",
      "examples": {
        "match": [
          "<fragmento real de un mail que confirma la entrega>"
        ],
        "no_match": [
          "<fragmento real de un mail que NO confirma la entrega>"
        ]
      },
      "extract": {
        "type": "object",
        "properties": {
          "deliveryDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Fecha de entrega en formato YYYY-MM-DD. Resolver el año o fechas relativas usando la fecha de recepción del mail."
          },
          "deliveryAddress": {
            "type": "string",
            "description": "Domicilio de entrega completo tal como aparece en el mail"
          }
        },
        "required": ["deliveryDate", "deliveryAddress"],
        "additionalProperties": false
      }
    }
  ],
  "default-outcome": {
    "label": "meds.unknown"
  }
}
