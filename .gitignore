output.txt
node_modules
dist
.mastracode
.mastra
.env
.env.*
*.db
*.db-*
*.duckdb
*.duckdb.*
.netlify
.vercel

# Google OAuth client credentials (nunca commitear)
client_secret*.json
.infisical.json

# Reglas reales del clasificador (contienen texto de mails del proveedor)
secrets/
docs/*.txt
